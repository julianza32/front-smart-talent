import { FilterRoomsDartPipe } from './filter-rooms.dart.pipe';

describe('FilterRoomsDartPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRoomsDartPipe();
    expect(pipe).toBeTruthy();
  });
});
